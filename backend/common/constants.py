STORAGE_DIR = "storage" # generic directory to cache the generated index
STORAGE_DIR_FOODS = "storage/food-recipes"  # directory to cache the generated index for food recipes
DATA_DIR_FOODS = "resources/data/food-recipes"  # directory containing the documents to index for food recipes
STORAGE_DIR_DRINKS = "storage/drink-recipes"  # directory to cache the generated index for drink recipes
DATA_DIR_DRINKS = "resources/data/drink-recipes"  # directory containing the documents to index for drink recipes
CHUNK_SIZE = 1024
CHUNK_OVERLAP = 20
